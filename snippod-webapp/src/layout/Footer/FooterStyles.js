import semanticVar from 'theme/semantic-variables';

const styles = {
  layout: {
    padding: '2rem 2rem'
  },

  githubButton: {
    verticalAlign: 'text-bottom',
    marginTop: '0.8em'
  },

  lineHeight: {
    lineHeight: '1.8em'
  }
};

module.exports = styles;
