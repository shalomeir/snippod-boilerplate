// This optional file is used to load the KSS template engine needed by this
// template. The filename should follow standard node.js conventions, so it
// should either have the same name as the enclosing folder (e.g.
// parentfolder.js) or it should be named index.js.

var KssHandlebarsEngine = require('kss/cli/handlebarsEngine');

// Tell kss-node which template engine this template uses.
module.exports.engine = KssHandlebarsEngine;
